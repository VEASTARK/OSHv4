sourceSets {
    main {
        java {
            srcDir 'src'
            srcDir 'src_sim'
        }
    }
}

dependencies {
    implementation project(":osh_core")

    implementation group: 'it.unimi.dsi', name: 'fastutil'
}