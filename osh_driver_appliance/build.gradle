sourceSets {
    main {
        java {
            srcDir 'src'
            srcDir 'src_sim'
            srcDir 'src_toolbox'
        }
    }
}

dependencies {
    compile project(":osh_core")
    compile project(":osh_en50523")
}