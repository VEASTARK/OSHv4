sourceSets {
    main {
        java {
            srcDir 'src_sim'
        }
    }
}

dependencies {
    implementation project(":osh_core")

    implementation group: 'javax.xml.bind', name: 'jaxb-api'
}